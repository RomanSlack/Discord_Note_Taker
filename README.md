### Quick start
```bash
git clone <repo_url>
cd discord-meeting-recorder
cp .env.example .env        # add keys
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python bot.py